@using WebCarRentalSystem.ViewModels
@model CreateCarViewModel

<body>
    <form method="post" asp-action="Create">
        <div class="border p-3 mt-4">
            <div class="row pb-2">
                <h2 class="text-primary">Create Car</h2>
                <hr />
            </div>
            <div class="mb-3">
                <label asp-for="ModelCarId"></label>
                <input asp-for="ModelCarId" class="form-control" placeholder="Model ID" />
                <span asp-validation-for="ModelCarId" class="text-danger"></span>

@*                <select class="form-control"
                        asp-for="Car.ModelCarId" disabled class="form-control"
                        asp-items="@(new SelectList(Model.Models, "Id", "Marka"))">
                        <option value="" disabled selected>Choose a marka</option>
                </select>
                <span asp-validation-for="Car.ModelCarId" class="text-danger d-block"></span>*@
@*
                <label asp-for="ModelCarId"></label>
                <select asp-for="Car.Model.Marka" class="form-select"
                        asp-items="@(new SelectList(Model.Models.Marka))">
                        <option value="" disabled selected>Choose Marka</option>
                </select>*@

            </div>
            <div class="mb-3">
                <label asp-for="Color"></label>
                <input asp-for="Color" class="form-control" placeholder="Color" />
                <span asp-validation-for="Color" class="text-danger"></span>
            </div>
            <div class="mb-3">
                <label asp-for="Rented"></label>
                <select asp-for="Rented" class="form-select">
                    <option value="" disabled selected>Choose Status</option>
                    <option>True</option>
                    <option>False</option>
                </select>
            </div>
            <div class="mb-3">
                <label asp-for="CarRegNumber"></label>
                <input asp-for="CarRegNumber" class="form-control" placeholder="AB01AB1234" />
                <span asp-validation-for="CarRegNumber" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-primary" style="width:150px">Create</button>
            <a asp-controller="Car" asp-action="Index" class="btn btn-secondary" style="width:150px">
                Back to List
            </a>
        </div>
    </form>
</body>

@section Scripts{
    @{
        <partial name="_ValidationScriptsPartial" />
    }
}
